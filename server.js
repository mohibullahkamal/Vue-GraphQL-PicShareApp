const { ApolloServer } = require('apollo-server');

const server = new ApolloServer({}); //We innitialize Apollo server here

server.listen; //now that we have innitialised Apollo server; we can call a method on it
